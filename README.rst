samovar
=======

``samovar`` is a tool managing multiple workspaces with multiple repositories
in each of them.

It performs bulk ``hg`` operations on all the repositories (if not instructed
otherwise) from the workspace.
 